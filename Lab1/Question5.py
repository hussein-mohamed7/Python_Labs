def compute_Area(base,height):
    return (1/2) * base * height

base = float(input("Enter base : "))
height = float(input("Enter height : "))
result = compute_Area(base,height)
print(result)
    
    